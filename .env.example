# Exam Display Configuration

# Timezone for the exam display
# Use IANA timezone identifiers (e.g., 'Asia/Jakarta', 'America/New_York', 'Europe/London', 'UTC')
# Default: UTC (if not specified)
TIMEZONE=UTC

# NTP Server for time synchronization
# Leave empty to use local server time, or specify an NTP server hostname/IP
# Examples: ntp.ui.ac.id, pool.ntp.org, time.google.com, time.cloudflare.com
# Default: empty (uses local server time)
NTP_SERVER=

# Application Port (for development)
# Default: 3000 (Vite will use 5173+ for dev server)
PORT=3000

# Node Environment
NODE_ENV=production
